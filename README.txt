Prerequisites:

1) download latest version anaconda (add python to classpath checkbox)
2) open console `cmd`, `pip install jupyterlab pandas geopandas`
3) create project folder, `noch-vub-workshop`
3) `jupyter notebook`

conda install folium -c conda-forge
conda install descartes 

conda install -c conda-forge pyproj=2.1.0
conda install -c conda-forge pyproj=2.5.0
4) download dataset csv, https://github.com/tonykoval/noch-vub-workshop (create data folder, zip and unzip project)
